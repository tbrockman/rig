# Built binary. Anchored, or this also matches cmd/rig.
/rig 
.claude

# Guest flakes in this checkout lock against base/, which changes with every
# edit to it; a committed lock would be stale by the next commit.
/project-template/guest/flake.lock
/examples/*/guest/flake.lock
